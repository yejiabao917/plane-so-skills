interface:
  display_name: "Plane.so Skills"
  short_description: "Operate Plane work items, labels, members, and comments through the official API."
  default_prompt: "Use $plane-so-skills to operate Plane work items directly from the user's task details."
