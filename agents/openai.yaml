interface:
  display_name: "Plane.so Skills"
  short_description: "Create Plane work items from natural-language task details."
  default_prompt: "Use $plane-so-skills to create a Plane work item directly from the user's task details."
